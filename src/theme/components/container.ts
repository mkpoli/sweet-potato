const baseStyle = {
  w: '100%',
  maxW: '1024px',
  px: [2, 4, 5],
};

const variants = {
  default: {},
};

const defaultProps = {
  variant: 'default',
};

export default {
  baseStyle,
  variants,
  defaultProps,
};
