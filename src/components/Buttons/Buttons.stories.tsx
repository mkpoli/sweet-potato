import React from 'react';
import { Button } from '@chakra-ui/react';
export default { title: 'Buttons' };

export const defaultStyle = (): JSX.Element => (
  <Button bgColor="potato" color="white">
    Button
  </Button>
);
